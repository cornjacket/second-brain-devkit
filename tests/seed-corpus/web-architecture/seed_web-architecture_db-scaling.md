---
tags: [seed, web-architecture]
---

# Database scaling

Databases scale by reading from replicas, sharding data across nodes, or partitioning tables. Each choice trades consistency, complexity, and cost. Most systems scale reads long before they must scale writes.
