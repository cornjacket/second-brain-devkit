---
tags: [seed, web-architecture]
---

# Caching

Caching stores expensive results closer to the consumer — in memory, a CDN, or the browser — to cut latency and load. The hard parts are invalidation and staleness. Cache keys and TTLs encode the freshness trade-off.
