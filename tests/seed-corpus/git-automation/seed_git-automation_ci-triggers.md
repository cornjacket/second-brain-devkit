---
tags: [seed, git-automation]
---

# Triggering CI

CI systems run pipelines on push and pull-request events, gating merges on green checks. Branch and path filters limit when a workflow runs. Required status checks enforce that tests pass before merge.
