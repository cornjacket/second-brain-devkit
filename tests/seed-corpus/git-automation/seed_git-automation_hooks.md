---
tags: [seed, git-automation]
---

# Git hooks

Hooks are scripts Git runs at lifecycle points — pre-commit, post-commit, pre-push — to lint, test, or generate derived files automatically. A pre-commit hook can block a bad commit; a post-commit hook runs after and cannot. Set core.hooksPath to version them with the repo.
