---
tags: [seed, git-automation]
---

# Submodules

A submodule embeds another repository at a pinned commit inside a parent repo, keeping their histories separate. The parent records which commit of the child it depends on. They add coordination overhead, so use them deliberately.
