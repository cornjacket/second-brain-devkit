---
tags: [seed, git-automation]
---

# Worktrees

git worktree checks out multiple branches into separate directories sharing one repository, so you can work on several without stashing. Each worktree has its own working files and HEAD. It suits parallel builds or reviewing a branch while coding another.
