---
tags: [seed, git-automation]
---

# Rebase vs merge

Rebase replays commits onto a new base for a linear history, while merge preserves the branch topology with a merge commit. Interactive rebase can squash, reorder, and reword before sharing. Avoid rebasing commits others have already pulled.
