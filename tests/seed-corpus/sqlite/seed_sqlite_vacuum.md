---
tags: [seed, sqlite]
---

# VACUUM and maintenance

VACUUM rebuilds the database file to reclaim space left by deleted rows and reduce fragmentation. ANALYZE updates statistics the query planner relies on. Auto-vacuum can reclaim space incrementally instead.
