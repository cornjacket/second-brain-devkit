---
tags: [seed, sqlite]
---

# JSON support

SQLite's JSON functions let you store and query JSON text with operators like json_extract and the -> and ->> shorthands. It suits semi-structured columns without a separate document store. Generated columns can index extracted JSON fields.
