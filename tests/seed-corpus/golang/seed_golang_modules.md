---
tags: [seed, golang]
---

# Modules and packages

Go modules pin dependencies and versions in go.mod, replacing the old GOPATH workflow. A module groups packages under a path; go get and go mod tidy manage the graph. Semantic import versioning encodes major versions in the path.
