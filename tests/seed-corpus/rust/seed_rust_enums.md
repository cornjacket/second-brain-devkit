---
tags: [seed, rust]
---

# Enums and pattern matching

Rust enums are algebraic data types whose variants can carry data, and match exhaustively destructures them. Option and Result are enums that encode absence and failure in the type system. The compiler forces you to handle every variant.
