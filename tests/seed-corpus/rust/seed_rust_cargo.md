---
tags: [seed, rust]
---

# Cargo and crates

Cargo is Rust's build tool and package manager; Cargo.toml declares dependencies (crates) and Cargo.lock pins exact versions. It handles building, testing, and publishing to crates.io. Workspaces group related crates under one build.
