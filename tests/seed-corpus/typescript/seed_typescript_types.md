---
tags: [seed, typescript]
---

# Structural typing

TypeScript uses structural typing: compatibility is based on shape, not declared names, so any object with the right members fits. This makes duck typing safe at compile time. Types are erased at build time and add no runtime cost.
