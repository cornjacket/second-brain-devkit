---
tags: [seed, typescript]
---

# Generics

Generics parameterize types over other types, so a function like identity<T> preserves the caller's type through its return. Constraints via extends limit what a parameter can be. They enable reusable, type-safe containers and utilities.
