---
tags: [seed, typescript]
---

# Modules

ES modules use import and export, and TypeScript resolves them with configurable module resolution strategies. Type-only imports drop at build time. Bundlers and tree-shaking rely on the static structure of module imports.
