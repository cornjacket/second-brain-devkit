---
type: glossary
tags: [glossary]
---

# Term

Term ? A one-line definition — state the meaning atomically, one term, one idea.

#flashcards/glossary

<!--
Glossary term template — the controlled-vocabulary layer, the G in PARA(G).
------------------------------------------------------------------------
1. Copy into vault/glossary/ renamed to the term in lowercase-kebab-case.md
   (e.g. retrieval-substrates.md), or run: python3 scripts/glossary_new.py "<term>"
2. Keep `type: glossary` — it is the tool-facing marker (scan, flashcards, graph).
3. Replace both "Term" (the # title and the card front) with the actual term, and
   fill in the definition after the `?`.
4. The `Term ? <definition>` line + the `#flashcards/…` tag make this a valid card
   for the community Spaced Repetition plugin out of the box.
5. Delete this comment and commit.

This note lives in vault/glossary/ (NOT a PARA root), so it is never embedded or
returned by semantic search — its value is the link graph. See vault/glossary/README.md.
-->
